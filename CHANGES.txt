RSL Controller v01 Enhanced Motor Control - 03/31/26
=====================================================

1. Absolute Position Control Implementation
   - Added direct degree-based positioning for azimuth and elevation
   - New GUI input fields for target position entry
   - "Go To Az Position" and "Go To El Position" buttons
   - Position entries support decimal degrees (e.g., 45.5°)
   - CCW (counter-clockwise) rotation defined as positive for azimuth
   - Positions calculated relative to home (0°, 0°) reference
   - Software home offset automatically applied in absolute positioning
   - Validation ensures elevation targets remain within safe limits

2. Incremental Movement System
   - All motor movements broken into 2-step increments
   - Position queried and updated after each 2-step movement
   - Constant INCREMENTAL_STEP_SIZE = 2 for fine-grained tracking
   - Progress percentage displayed in status bar during movement
   - Current position in degrees shown throughout movement
   - Enables monitoring of signal changes during antenna rotation
   - Applies to both relative (manual) and absolute positioning modes
   - Smoother visual feedback and more responsive position tracking

3. Enhanced Motor Movement Functions
   - Created incremental_move() method for chunked movements
     * Takes total steps and breaks into 2-step increments
     * Logs each increment for debugging
     * Updates GUI after each increment with root.update_idletasks()
     * Verifies position changed after first increment
     * Returns success/failure with position verification
     * Allows 1-step tolerance for final position accuracy
   - Updated move_azimuth_absolute() to use incremental movements
     * Calculates relative movement from current position
     * Displays target and current positions during move
     * Reports final position after completion
   - Updated move_elevation_absolute() to use incremental movements
     * Same incremental approach as azimuth
     * Enforces elevation limits during movement
   - Updated manual_move() to use incremental movements
     * Relative movements now tracked with progress updates
     * Applies to all CCW/CW and Up/Down button operations

4. Elevation Limit Expansion
   - Changed elevation limits from ±20° to ±75°
   - Updated goto_absolute_elevation() validation (line 829)
   - Updated warning label in GUI to show new ±75° limits (line 193)
   - Updated help text and documentation throughout
   - Provides much greater vertical scanning range
   - Supports hemispherical radiation pattern measurements

5. Auto-Connect Motor on Startup
   - Added auto_connect_motor() method (line 448)
   - Automatically attempts connection 500ms after GUI starts
   - Connects to default port /dev/ttyACM0 on launch
   - Eliminates manual connection step for standard setup
   - Status feedback during auto-connection attempt
   - Only attempts if port is available and not already connected
   - Falls back to manual connection if auto-connect fails

6. GUI Layout Reorganization
   - Motor controls moved to top of window (most prominent position)
   - Layout order now:
     * Title and subtitle
     * Motor Control section (rows 0-12 in manual_frame)
     * Measurement Settings
     * Output Settings
     * Monitor Control buttons
     * Status bar
     * VNA Connection Settings (moved to bottom)
     * Motor Controller Connection (moved to bottom)
     * Information panel
   - Current Position display is first element in Motor Control
   - Absolute Position controls second (more prominent than relative)
   - Relative Movement controls third
   - Quick actions (Home, Emergency Stop) fourth
   - Connection settings de-emphasized at bottom of scrollable area

7. Position Display Improvements
   - Large, bold position display with color coding
     * Azimuth in green (16pt bold font)
     * Elevation in blue (16pt bold font)
   - Real-time updates during all movements
   - Progress tracking shows percentage and current position
   - Status bar displays movement state and completion percentage
   - GUI remains responsive during long movements (update_idletasks())
   - Position verification after each increment

8. Updated Documentation
   - File header updated with all new features (lines 1-16)
   - Info panel updated with key features (lines 301-316)
   - Help text expanded with incremental movement section (lines 782-841)
   - Motor controls documentation enhanced (lines 820-841)
   - Added INCREMENTAL MOVEMENT section to help
   - Updated all references from 5-step to 2-step increments
   - Changed elevation limit references throughout

9. Movement Validation and Error Handling
   - Position-based success verification instead of response codes
   - Tolerance checking (1-step tolerance for 2-step increments)
   - Direction validation (movement in correct direction counts as success)
   - Detailed logging of each movement increment
   - Response logging for debugging
   - Motor timeout increased to 0.2s per increment for reliability
   - GUI error dialogs for connection failures and limit violations

10. Code Organization and Optimization
   - INCREMENTAL_STEP_SIZE constant for easy adjustment (line 35)
   - Consolidated movement logic in incremental_move() method
   - Reduced code duplication across movement functions
   - Clear separation of relative vs absolute positioning
   - Software home offset handling in both modes
   - Thread-safe GUI updates throughout movement operations
   - Debug output with [Motor] prefix for all operations

11. User Experience Enhancements
   - Prominent position display catches user attention immediately
   - Absolute positioning more accessible than relative controls
   - Auto-connect eliminates setup step
   - Real-time progress reduces uncertainty during movements
   - Visual feedback at every 2-step increment
   - Connection settings hidden at bottom (rarely changed after setup)
   - Larger elevation range enables more comprehensive measurements
   - CCW = Positive convention clearly stated in GUI labels

Usage:
   - GUI: python3 scripts/RSL_controller_v01.py
   - Motor auto-connects on startup to /dev/ttyACM0
   - Enter target degrees in "Absolute Position" section
   - Click "Go To Az Position" or "Go To El Position"
   - Watch real-time position updates during movement
   - Use relative controls for fine adjustments
   - Monitor starts automatically when motor connects
   - All movements tracked in 2-step increments

Technical Notes:
   - Step-to-degree conversion: 200 steps = 360° (1.8° per step)
   - 2-step increment = ~3.6° per update
   - Position updates every 0.2 seconds during movement
   - GUI refreshes with update_idletasks() for responsiveness
   - Works with both hardware SETZERO and software home calibration


Real-Time Monitor with Motor Control Integration - 03/11/26
============================================================

1. Created Motor Control Version of Real-Time Monitor
   - New files: znle_realtime_monitor_motor_controls.py and znle_realtime_monitor_motor_controls_gui.py
   - Separate version preserving original non-motor files
   - Desktop launcher: ZNLE6_Realtime_Monitor_Motor_Controls.desktop
   - Combines VNA real-time monitoring with Arduino motor control
   - Independent GUI sections: "Monitor Control" and "Manual Motor Control"
   - Test utility: test_motor_connection.py for Arduino debugging

2. Motor Control Integration
   - Integrated full motor control system from antenna_aligner_gui.py
   - Serial port scanning and connection via Motor Controller section
   - Arduino communication at 115200 baud rate
   - Supported commands: AZ, EL, AZABS, ELABS, HOME, SETZERO, STOP, STATUS, SPEED
   - Manual Motor Control section with absolute/relative movement
   - Real-time position display for azimuth and elevation
   - Non-blocking motor connection using threading
   - Status feedback for all motor operations

3. Fixed Motor Communication Bugs
   - Threading fix: Moved connection to background thread to prevent GUI freeze
   - Command bug: Fixed critical '\n' vs '\\n' newline character error
   - Timeout handling: Added 1-second timeout with polling loop for responses
   - Debug output: Added [Motor] prefix for all motor operations
   - Connection callbacks: connection_success() and connection_failed() methods
   - Serial buffer clearing for clean communication

4. Home Position Calibration Feature
   - Added "Set Current as Home" button for home position calibration
   - Primary method: SETZERO command (hardware-based zeroing)
   - Automatic command detection and error handling
   - Position tracking with offset support for software mode
   - Goto Home button respects calibrated home position

5. Software Home Calibration Fallback
   - Implemented software-based home calibration for older Arduino firmware
   - Automatically activates when hardware SETZERO command not supported
   - Variables: use_software_home, home_offset_az, home_offset_el
   - Applies offsets in update_motor_position() method
   - Transparent operation - user experience identical for both modes
   - Status message indicates which mode is active
   - Works with any firmware version

6. Fixed Y-Axis Auto-Scaling on Real-Time Plot
   - Issue: Y-axis didn't adjust for significant amplitude jumps
   - Root cause 1: Scaling centered on mean instead of actual data range
   - Root cause 2: FuncAnimation blit=True caching axes background
   - Solution 1: Changed to min/max based scaling with proportional margins
     * Uses actual minimum and maximum values from data
     * 15% margin above and below (minimum 2 dB)
     * Fixed 5 dB margin for small ranges (<5 dB)
   - Solution 2: Changed blit=False to allow full axes redraw
   - Result: Y-axis now dynamically adjusts to show full signal range

7. Motor Control Architecture
   - Dual-mode operation: hardware commands or software offsets
   - Position update thread for continuous monitoring
   - Thread-safe GUI updates using root.after()
   - Graceful error handling for connection failures
   - Status display with connection state and current position
   - Emergency stop button for safety
   - Speed control (1-10 units)

8. File Organization
   - Original files preserved: znle_realtime_monitor.py, znle_realtime_monitor_gui.py, ZNLE6_Realtime_Monitor.desktop
   - Motor control versions separate and independent
   - Documentation: arduino/FIRMWARE_UPDATE_INSTRUCTIONS.md
   - CSV output structure ready for motor angle logging (TODO markers in place)

9. Documentation and Testing
   - Created test_motor_connection.py for Arduino verification
   - Interactive command testing with SETZERO support
   - FIRMWARE_UPDATE_INSTRUCTIONS.md for upgrading Arduino code
   - Debug output for troubleshooting motor communication
   - Comprehensive error messages for connection issues

10. Use Cases
   - Manual antenna alignment while monitoring signal in real-time
   - Testing motor positioning accuracy at specific signal conditions
   - Calibrating home position for consistent reference point
   - Debugging Arduino motor controller communication
   - Future: Automated scanning patterns with live signal monitoring

Usage:
   - GUI: python3 scripts/znle_realtime_monitor_motor_controls_gui.py
   - Desktop: Double-click "ZNLE6 Real-Time Monitor with Motor Control" icon
   - Testing: python3 scripts/test_motor_connection.py


Real-Time Signal Monitor with Smoothing - 03/10/26
===================================================

1. Created Real-Time Signal Monitor (znle_realtime_monitor.py)
   - New continuous monitoring tool with live matplotlib animation
   - Measures signal amplitude at single target frequency repeatedly
   - Real-time plot display: amplitude (y-axis) vs time (x-axis)
   - Live-updating graph using matplotlib FuncAnimation
   - In-memory buffer (deque) for efficient rolling data storage
   - Maximum 1000 points displayed on plot (older data auto-removed)
   - Continuous CSV logging of all measurements with timestamps
   - Graceful shutdown on Ctrl+C or plot window close

2. Real-Time Monitor GUI (znle_realtime_monitor_gui.py)
   - Graphical interface for easy configuration and launching
   - Connection settings: IP address and port (same as ZNLE Controller)
   - Measurement settings:
     * Target frequency input (single frequency in Hz)
     * S-parameter dropdown: S11, S12, S21, S22
     * Update interval: configurable measurement rate (100-10000 ms)
     * Smoothing window: rolling average of N samples (1-100)
   - Output settings: CSV directory and filename (same as other tools)
   - Control buttons: Start, Stop, Help
   - Information panel with usage tips

3. Smoothing Functionality
   - Rolling average smoothing to reduce measurement noise
   - Configurable smoothing window parameter (default: 10 samples)
   - GUI input field with validation (1-100 samples)
   - Dual data storage:
     * raw_amplitudes: original measurements preserved
     * amplitudes: smoothed values for plotting
   - Enhanced CSV output with both raw and smoothed columns:
     * Amplitude_Raw_dB: original VNA measurement
     * Amplitude_Smoothed_dB: rolling average of last N samples
   - Real-time smoothing calculation using numpy.mean()
   - Plot displays smoothed data for cleaner visualization
   - Plot title indicates smoothing status: "(smoothed: N samples)"
   - Info box shows both current smoothed and raw values
   - Console output displays both raw and smoothed amplitudes
   - Smoothing window = 1 disables smoothing (plots raw data)

4. Live Plotting Architecture
   - matplotlib FuncAnimation for continuous updates
   - Data flow: VNA → memory buffer → plot (not file-based)
   - Parallel CSV writing for data persistence
   - No disk I/O delay in plotting loop
   - Auto-scaling y-axis based on signal range
   - Expanding x-axis as time progresses
   - Efficient FIFO buffer using collections.deque

5. VNA Configuration for Real-Time Monitoring
   - Single frequency point measurement (CW mode)
   - Averaging disabled (SENS:AVER OFF) for fast updates
   - Log magnitude format (dB)
   - ASCII data format for easy parsing
   - Manual trigger mode (INIT:CONT OFF)
   - Configurable S-parameter selection
   - 10 second timeout per measurement

6. Data Output and Logging
   - CSV format: Timestamp, Elapsed_Time_s, Frequency_Hz, Amplitude_Raw_dB, Amplitude_Smoothed_dB
   - Timestamped filenames: {filename}_{timestamp}.csv
   - Millisecond precision timestamps
   - All measurements logged regardless of plot display limit
   - Data preserved even when plot buffer rolls over (>1000 points)
   - CSV written and flushed after each measurement for data safety

7. Desktop Integration
   - Created ZNLE6_Realtime_Monitor.desktop launcher
   - Single-click access from desktop icon
   - Uses virtual environment Python interpreter
   - Icon: applications-science
   - Categories: Science, Education, Engineering

8. Documentation
   - Created README_realtime_monitor.md with comprehensive guide
   - Usage examples for GUI and command line
   - S-parameter selection guide for different hardware setups
   - Hardware configuration examples (two-antenna and single-antenna)
   - Troubleshooting section
   - Performance notes and optimization tips
   - Data flow architecture explanation

9. Use Cases
   - Antenna alignment: monitor signal while adjusting position
   - Signal stability testing: track variations over time
   - Dynamic signal tracking: follow rapidly changing signals
   - Amplifier testing: verify gain stability
   - Link quality monitoring: continuous transmission monitoring

10. Technical Features
   - Configurable update interval (default: 500ms = 2 Hz)
   - Non-blocking operation using threading
   - Thread-safe GUI updates with root.after()
   - Automatic resource cleanup on shutdown
   - VNA connection verification with *IDN? query
   - Error handling for measurement failures
   - Process management for graceful termination

Usage:
   - GUI: python3 znle_realtime_monitor_gui.py
   - Command line: python3 znle_realtime_monitor.py --freq 977e6 --param S21 --smoothing 10
   - Desktop: Double-click "ZNLE6 Real-Time Monitor" icon


Frequency Scanner Timeout Fix and S-Parameter Selection - 03/10/26
===================================================================

1. Fixed VNA Timeout Error (find_quiet_frequencies.py)
   - Increased timeout from 20 to 60 seconds to fix VI_ERROR_TMO error
   - Timeout now sufficient for 401-point scans on ZNLE6 VNA
   - Added progress messages to identify which stage is executing:
     * "Querying instrument identification..."
     * "Performing frequency sweep (401 points)..."
     * "Waiting for sweep to complete..."
     * "Retrieving data from VNA..."
   - Enables better diagnosis of timeout locations in future

2. Measurement Speed Optimization
   - Added SENS:AVER OFF command to disable VNA averaging
   - Significantly reduces sweep time for multi-point scans
   - Maintains measurement accuracy for signal level detection
   - Critical for responsive operation with 401+ measurement points

3. Removed Duplicate VNA Command
   - Fixed duplicate "INIT:CONT OFF" command in find_quiet_frequencies.py
   - Command was being sent twice consecutively (lines 266 and 269)
   - Removed redundancy without affecting functionality

4. Added S-Parameter Selection (frequency_scanner_gui.py + find_quiet_frequencies.py)
   - Added dropdown menu in GUI for S-parameter selection
   - Available options: S11, S12, S21, S22
   - Default value: S22 (reflection at port 2)
   - Helper text: "(S11/S22=Reflection, S21/S12=Transmission)"
   - GUI passes --s-parameter argument to backend script
   - Backend script displays selected S-parameter in scan header
   - VNA configured to measure specified S-parameter dynamically

5. S-Parameter Configuration Details
   - S11: Reflection from port 1
   - S12: Reverse transmission (port 2 → port 1)
   - S21: Forward transmission (port 1 → port 2)
   - S22: Reflection from port 2
   - Parameter selection enables proper measurement for different hardware setups:
     * S21 for transmitter on port 1 → receiver on port 2 (two-antenna transmission)
     * S22 for noise floor measurement at port 2 (single-antenna reception)
     * S11 for noise floor measurement at port 1

6. Hardware Configuration Context
   - Changes support new test setup with dual-antenna configuration
   - NanoVNA H4 transmitter (977 MHz) → 20dB LNA → UWB-12000 antenna (transmit)
   - UWB-12000 antenna (receive) → ZNLE6 port 2
   - S21 measurement captures signal transmission through both antennas
   - Previous S22 measurement only captured reflection at receiving port

7. User Interface Enhancements
   - S-parameter dropdown positioned after receiver port selection
   - Combobox set to read-only to prevent invalid entries
   - Selection validated and passed through command-line interface
   - Maintains consistency between GUI selection and backend execution


Antenna Aligner GUI Scrollbar Addition - 02/24/26
==================================================

1. Added Scrollable Interface to Combined GUI (antenna_aligner_gui.py)
   - Implemented vertical scrollbar for left control panel
   - Enables access to all GUI functions when window height is limited
   - Scrollbar features:
     * Vertical scrollbar automatically shows/hides based on content height
     * Mouse wheel scrolling support (Windows, Mac, Linux)
     * Canvas-based scrollable frame containing all control panels
     * Right plot display remains fixed and expandable
   - Improves usability on smaller displays and laptops
   - All control sections now accessible regardless of window size


Monitor Mode Plot Scaling Fix - 02/24/26
=========================================

1. Corrected Y-Axis Scaling in Monitor Mode (znle_pyvisa.py)
   - Fixed y-axis range from average ± 2.5 dB back to average ± 5 dB
   - Restores original specification for better visibility of signal variations
   - Provides wider viewing window for antenna alignment adjustments
   - Updated plot comments and console output to reflect ± 5 dB range
   - Affects time-series plots in monitor mode only (frequency sweep mode unchanged)


Antenna Aligner Combined GUI - 02/17/26
=========================================

1. Integrated VNA + Motor Control GUI (antenna_aligner_gui.py)
   - Created comprehensive GUI combining VNA monitoring with motor control
   - Features:
     * Dual connection management: VNA (TCP/IP) and Arduino (Serial)
     * Frequency monitoring modes:
       - Single frequency: Monitor specific frequency (e.g., 977 MHz)
       - Frequency range: Track peak amplitude across frequency range
     * Three scan modes:
       - Azimuth Sweep (1D): Horizontal rotation at fixed elevation
       - Elevation Sweep (1D): Vertical tilt at current azimuth
       - 2D Grid Scan: Complete azimuth × elevation mapping with heatmap
     * Manual motor control:
       - Azimuth: CCW/CW buttons with configurable step size
       - Elevation: Down/Up buttons with configurable step size
       - Real-time position display (Az, El)
       - Home button for (0, 0) return
       - Emergency stop function
     * Automated scanning:
       - Configurable start/stop positions and step size for both axes
       - Adjustable measurement delay for settling time
       - Real-time data acquisition and plotting
       - Scan mode automatically enables/disables relevant parameters
     * Real-time visualization:
       - 1D line plots for azimuth/elevation scans
       - 2D heatmap for grid scans with colorbar
       - Automatic peak detection and marking
       - Data point counter and scan mode display
     * Data export:
       - Save scan data to CSV (azimuth, elevation, amplitude)
       - Automatic plot export to PNG (line or heatmap)
       - Timestamped filenames with scan mode identifier
     * Activity logging with timestamps
     * Status indicators for scan progress
   
2. Operating Modes:
   - Single Frequency Mode: Fast monitoring at known transmitter frequency
   - Range Peak Tracking: Robust to frequency drift, tracks maximum across range
   - Azimuth Scan: Characterize horizontal radiation pattern
   - Elevation Scan: Characterize vertical radiation pattern
   - 2D Grid Scan: Complete 3D pattern visualization via heatmap
   
3. Scan Workflow:
   - Connect to VNA and motor controller
   - Configure frequency (single or range)
   - Optionally use manual controls to pre-position antenna
   - Select scan mode (azimuth, elevation, or 2D)
   - Set scan parameters (start, stop, step, delay)
   - Execute automated scan: Move → Wait → Measure → Record → Plot
   - Export results to CSV and PNG
   
4. Desktop Launcher (Antenna_Aligner.desktop)
   - Desktop shortcut for easy access
   - Uses virtual environment Python interpreter
   - Icon: applications-science
   - Categories: Science, Education, Engineering

5. Documentation Updates:
   - README_antenna_aligner.md: Comprehensive user guide
     * Added manual control section
     * Documented all three scan modes with use cases
     * Updated data format to include elevation column
     * Enhanced troubleshooting guide
   - QUICKSTART_antenna_aligner.txt: Quick reference card
     * Added manual control quick reference
     * Documented scan mode selection
     * Provided example configurations for all modes
     * Updated output file format

6. Technical Implementation:
   - Threading for non-blocking scans
   - Matplotlib TkAgg backend for GUI integration
   - Error handling for connection and measurement failures
   - Automatic VNA reconfiguration based on frequency mode
   - Position tracking from Arduino STATUS command
   - Dynamic UI element enabling/disabling based on scan mode
   - Support for 2D numpy arrays and heatmap generation
   - Raster scan pattern for 2D grid (row-by-row)

7. Use Cases:
   - Antenna radiation pattern characterization (1D and 2D)
   - Optimal receiver orientation determination (azimuth and elevation)
   - Signal strength vs. angle analysis
   - Directivity measurements
   - Beam width determination (horizontal and vertical)
   - Comparative testing of different antenna positions
   - Full spherical pattern approximation via grid scan


Motor Controller Development - 02/11/26
=========================================

1. Motor Controller GUI Application (motor_controller_gui.py)
   - Created comprehensive GUI for antenna stepper motor control
   - Features:
     * Automatic connection to Arduino on /dev/ttyACM0 at startup
     * Auto-detection of available serial ports with refresh capability
     * Real-time position display for azimuth and elevation
     * Auto-update status monitoring option
     * Adjustable motor speed control (1-100 RPM) with slider
     * Relative movement controls with directional buttons (CW/CCW, Up/Down)
     * Absolute positioning to target coordinates
     * Home position calibration (SETZERO command)
     * Emergency stop button
     * Activity log with timestamps
     * Automatic calibration prompt on startup
   - Connection management with disconnect confirmation on exit
   - Threading for non-blocking status updates

2. Desktop Launcher Integration (Motor_Controller.desktop)
   - Created desktop shortcut matching project style
   - Uses virtual environment Python interpreter
   - Icon: applications-engineering
   - Categories: Science, Education, Engineering

3. Arduino Firmware Enhancements (antenna_controller.ino)
   - Motor Position Holding: Removed motor release after movements
     * Motors now maintain holding torque to prevent position drift
     * Holding torque only released during emergency stop
   - Position Calibration: Added SETZERO command
     * Sets current position as new home (0, 0)
     * Allows calibration of horizontal receiver position
   - Elevation Safety Limits: Implemented ±20 step limits
     * MAX_ELEVATION = 20 steps (up from zero)
     * MIN_ELEVATION = -20 steps (down from zero)
     * Protects mounting hardware from geometry constraints
     * Returns error message if movement would exceed limits
   - Updated command documentation in header

4. Motor Controller Script Updates (motor_controller.py)
   - Base library unchanged (provides MotorController class)
   - Used as reference for GUI serial communication protocol

5. Default Configuration
   - Serial port: /dev/ttyACM0 (Arduino Uno R3)
   - Baudrate: 115200
   - Default step increment: 10 steps
   - Default motor speed: 30 RPM
   - Elevation range: -20 to +20 steps from calibrated home

6. User Workflow Improvements
   - Auto-connect eliminates manual connection step
   - Calibration prompt ensures proper home position setup
   - Small default step size (10) for precise adjustments
   - Visual warning about elevation limits in GUI
   - Confirmation dialogs for critical operations


Documentation Updates - 02/11/26
================================

1. Hardware Configuration Documentation (README.txt)
   - Added comprehensive Hardware Configuration section
   - Documented complete RF signal chain:
     * Transmitter: NanoVNA-H4 (977 MHz) → 40dB amplifier (5V) → RFSpace TSA900 UWB antenna
     * Receiver: Pasco WA-9800A (9V) with alligator clips → adapters → ZNLE6 Port 1
   - Detailed ZNLE6 VNA specifications and network settings
   - Documented Raspberry Pi 5 control computer configuration

2. Network Infrastructure Documentation
   - GL iNet AC1200 wireless router as network hub
   - ZNLE6 connected via Ethernet to router
   - Raspberry Pi 5, development PC, and application emulator via wireless
   - Router WAN connection to internet

3. Test Bed Apparatus Specifications
   - Detailed 3D printed receiver mount assembly
   - Two side brackets cradling Pasco WA-9800A receiver
   - U-shaped bracket arm for elevation control
   - NEMA17 stepper motor specifications:
     * Elevation control (Up-Down Axis): Left-side motor with keyed connection
     * Azimuth control (Rotational Axis): Center vertical motor
   - Base structure with 4 legs for stability


Frequency Scanner Tool and Enhancements - 02/06/26
==================================================

1. Created find_quiet_frequencies.py - RF Noise Floor Scanner
   - Scans frequency ranges to identify low-noise bands for RF transmission
   - Uses ZNLE6 VNA + Pasco WA-9800A receiver to measure background RF noise
   - Analyzes spectrum to find quiet (low-noise) frequency bands
   - Identifies bands below configurable noise threshold
   - Provides frequency recommendations for NanoVNA transmission experiments

2. Frequency Scanner Features
   - Dual frequency input modes: Center+Span or Start+Stop
   - Configurable scan parameters: points, threshold, minimum bandwidth
   - Automatic quiet band detection with configurable sensitivity
   - Statistical analysis: mean, median, min, max, standard deviation
   - Visual identification of quiet bands (highlighted in green on plots)
   - CSV export option for raw frequency vs power data
   - Comprehensive terminal output with recommendations

3. Created frequency_scanner_gui.py - GUI for Frequency Scanner
   - Graphical interface for find_quiet_frequencies.py
   - Connection settings: IP address, port, receiver port selection
   - Frequency mode toggle: Center+Span or Start+Stop
   - Quick preset buttons: 900 MHz ±100 MHz, 2.4 GHz ±100 MHz, 5 GHz ±200 MHz
   - Scan parameters: points, quiet threshold (dB), minimum bandwidth (MHz)
   - Custom filename input for organized data storage
   - Separate directory selectors for CSV and plot outputs
   - Real-time scan output display with scrollable text area
   - Help button with built-in documentation
   - Start/Stop buttons with status feedback

4. Desktop Integration
   - Created Frequency_Scanner.desktop launcher
   - Single-click access from desktop icon
   - Saved in repository at scripts/Frequency_Scanner.desktop
   - Configured with virtual environment Python interpreter
   - Proper working directory and startup settings

5. GitHub Connection and Repository Fixes
   - Set up automatic GitHub authentication using GitHub CLI (gh)
   - Configured git to use gh credentials with "gh auth setup-git"
   - Fixed corrupted git repository objects
   - Created recover_git_repo.sh script for future repository repairs
   - Recovered from empty object file errors
   - Repository now properly connected to github.com/lesmalan/Antenna_Aligner.git
   - GitHub authentication persists across system reboots

6. Monitor Mode Y-Axis Scaling Enhancement (znle_pyvisa.py)
   - Updated time-series plots to use consistent y-axis scaling
   - Y-axis now set to average ± 2.5 dB for all monitor mode measurements
   - Calculates average amplitude across entire measurement duration
   - Displays average and y-axis range in console output
   - Enables direct visual comparison between different alignment runs
   - Prevents auto-scaling from hiding small signal variations

7. Output File Management Improvements
   - Added --csv-dir, --plots-dir, --filename arguments to find_quiet_frequencies.py
   - Fixed plot/CSV saving to respect user-specified directories
   - Files now saved with custom base filename plus timestamp
   - GUI properly passes directory and filename parameters to backend script
   - Format: {filename}_{timestamp}.csv and {filename}_{timestamp}.png
   - Directories automatically created if they don't exist

8. Documentation and Change Tracking
   - Created vscode_github_commands.txt with GitHub connection instructions
   - Documented authentication setup for VS Code integration
   - Recorded all changes in CHANGES.txt following established format conventions
   - Preserved complete history of tools created and modifications made

9. Enhanced Error Reporting (frequency_scanner_gui.py)
   - Implemented comprehensive input validation with field-specific error messages
   - Added stderr capture for subprocess error handling
   - Categorized exceptions: FileNotFoundError, PermissionError, general exceptions
   - Added full traceback logging to scan output display
   - Provides examples of valid input formats in error messages
   - Validates IP address, port numbers, frequency values, and scan parameters
   - Shows specific field names and constraints when validation fails

10. Bug Fix - Missing Import Statement (find_quiet_frequencies.py)
   - Added missing "import os" at line 13
   - Fixed NameError that prevented directory creation and file operations
   - Required for os.path.join() and os.makedirs() function calls

11. Plot Enhancement - Quietest Frequency Marker
   - Added red star marker on plot to indicate quietest detected frequency
   - Marker placed at minimum power point across entire spectrum
   - Legend entry: "Quietest: {frequency} MHz at {power} dBm"
   - Provides quick visual identification of optimal transmission frequency

12. Non-Blocking Plot Display (find_quiet_frequencies.py)
   - Removed plt.show() which was blocking script execution
   - Script now completes immediately after scan finishes
   - Replaced with plt.close() to free memory resources
   - Added automatic plot opening using xdg-open subprocess
   - Plot opens in default image viewer as separate non-blocking process
   - GUI now shows "Scan complete!" message immediately
   - Enables continuous scanning operations without manual window management
   - Fixes issue where scan appeared frozen until matplotlib window was closed

Usage - Frequency Scanner:
   - Launch GUI: Double-click "Frequency Scanner" desktop icon
   - Or from terminal: python3 scripts/frequency_scanner_gui.py
   - Command line: python3 scripts/find_quiet_frequencies.py --center 900e6 --span 200e6
   - Select frequency range (presets or custom)
   - Configure scan parameters and output filename
   - Click "Start Scan" to begin measurement
   - View real-time progress and results
   - Check Plots/ directory for visual analysis
   - Use recommended frequency for NanoVNA transmission experiments

Dependencies Added:
   - numpy (for statistical analysis and array operations)
   - subprocess (Python standard library, for automatic plot opening)
   - All matplotlib and pyvisa dependencies already present from previous tools


Code Optimization and Documentation - 02/06/26
================================================

1. Comprehensive Code Documentation
   - Added undergraduate engineering level comments to all major scripts
   - Created dated backup files (.backup_20260206) before modifications
   - Added section headers with visual separators for code organization
   - Detailed explanations of RF concepts, SCPI commands, and data flow

2. znle_pyvisa.py Optimization (194 → 281 lines)
   - Added comprehensive header explaining VNA control and two operating modes
   - Created section dividers: Import Dependencies, Helper Functions, Main Program
   - Consolidated duplicate code by creating get_plot_path() helper function
   - Enhanced import statements with educational comments
   - Documented SCPI commands (FORM:DATA, CALC:PAR:DEF, SENS:FREQ, etc.)
   - Explained S-parameter measurements and dB scaling
   - Clarified difference between Frequency Sweep and Monitor modes
   - Added comments explaining fixed y-axis scaling (±2.5dB from average)

3. find_quiet_frequencies.py Optimization (299 → 379 lines)
   - Added comprehensive header explaining RF noise floor scanning concept
   - Improved section organization: Imports, Helper Functions, Analysis, Main Program
   - Enhanced hz_to_str() function documentation with examples
   - Detailed explanation of quiet band detection algorithm:
     * Median-based noise baseline calculation
     * Threshold detection methodology
     * Contiguous band identification logic
     * Minimum bandwidth filtering
   - Added comments to VNA configuration and measurement process
   - Documented statistical analysis approach
   - Explained plotting features (quietest frequency marker, green shading)
   - Clarified command-line argument groups and usage patterns

4. pi_tcp_server.py Optimization (207 → 256 lines)
   - Added comprehensive header explaining dual-protocol architecture
   - Removed duplicate code (eliminated redundant reap_jobs() definition)
   - Improved global state documentation (JOBS, WS_CLIENTS)
   - Enhanced TCP handler with detailed command documentation:
     * PING command (connection test)
     * STATUS command (job listing)
     * RUN command (VNA measurement launch)
     * QUIT/EXIT commands (connection close)
   - Added comments to WebSocket handler functions
   - Documented signal broadcasting mechanism (2 Hz update rate)
   - Explained background job cleanup thread operation
   - Added TODO note for future real RSL data integration

5. Code Quality Improvements
   - Better error handling documentation
   - Consistent formatting and structure across all scripts
   - Improved variable naming and function organization
   - Enhanced readability for future maintenance
   - Educational comments suitable for undergraduate engineering students

6. Documentation Benefits
   - New team members can understand code architecture quickly
   - RF/SCPI concepts explained for non-experts
   - Clear separation of concerns (imports, helpers, main logic)
   - Easier troubleshooting with well-documented sections
   - Suitable for educational use in engineering courses

7. Project Organization
   - Created scripts/Backups/ directory for version control
   - Moved all backup files to Backups directory:
     * znle_pyvisa.py.backup
     * znle_pyvisa.py.backup_20260206
     * find_quiet_frequencies.py.backup_20260206
     * pi_tcp_server.py.backup_20260206
     * znle_pyvisa_optimized.py (old version)
   - Clean scripts directory structure maintained

8. Testing and Validation
   - Verified all GUI applications launch correctly
   - Tested znle_gui.py functionality
   - Tested frequency_scanner_gui.py functionality
   - Confirmed all backend scripts load without errors
   - Validated all dependencies (pyvisa, matplotlib, numpy, tkinter)
   - Verified command-line argument parsing works correctly
   - All optimizations maintain backward compatibility


File Recovery from Commit Issues - 02/05/26
===========================================

1. Recovered znle_gui.py
   - File was lost in commit 49b68f4 ("whatever") on 02/04/26
   - Retrieved complete file from commit a595a1b (GUI_version01)
   - Restored all GUI functionality for VNA control
   - 404 lines of tkinter-based interface code recovered

2. Recovered CHANGES.txt Documentation
   - Lost entire "GUI Application for ZNLE6 Control - 02/04/26" section
   - Restored 83 lines of detailed change documentation
   - Recovered all 11 subsections documenting GUI features and updates
   - Retrieved from git commit history using git show

3. Recreated ZNLE6_Controller.desktop
   - Desktop launcher file existed on Desktop but not in repository
   - Copied launcher configuration to repository for version control
   - Preserves virtual environment Python path and working directory settings

4. Recreated Output Directories
   - Created CSVs/ directory for data file storage
   - Created Plots/ directory for graph image storage
   - Both directories referenced in GUI but were missing from workspace

5. Added Documentation for Raspberry Pi Server Integration
   - Documented pi_tcp_server.py and pi_websocket_server.py features
   - Added systemd service integration details
   - Recorded Flutter app WebSocket updates from 02/04/26
   - Ensured complete change history preservation


Bug Fixes - 02/05/26
====================

1. Fixed Plot File Output Directory
   - Corrected znle_pyvisa.py to respect PLOTS_DIR environment variable
   - Plots now save to Plots directory when GUI specifies separate directories
   - Previously plots were saved to CSV directory regardless of GUI settings
   - Added os.environ.get('PLOTS_DIR') check in both monitor and frequency modes
   - Maintains backward compatibility for command-line usage (saves alongside CSV)
   - GUI-specified directory separation now works correctly for both CSVs and Plots


GUI Application for ZNLE6 Control - 02/04/26
=========================================

1. Created znle_gui.py - Graphical User Interface
   - Built with tkinter for easy access to znle_pyvisa.py functionality
   - Provides user-friendly interface for spectrum analyzer control
   - Eliminates need for command-line arguments

2. GUI Features - Connection Settings
   - IP address and port configuration fields
   - Default values match instrument settings (192.168.15.90:5025)

3. GUI Features - Measurement Settings
   - Start/Stop frequency input fields (in Hz)
   - Number of sweep points configuration
   - S-parameter selection dropdown (S11, S21, S12, S22)
   - Optional sweep time setting

4. GUI Features - Operating Modes
   - "Run Frequency Mode" button: Single frequency sweep with amplitude plot
   - "Run Monitor Mode" button: Time-series monitoring at specific frequency
   - Monitor mode settings: frequency, duration (default 60s), interval (default 1s)
   - Stop button to cancel running measurements

5. GUI Features - User Interface
   - Output file selection with browse dialog
   - Real-time status display showing measurement progress
   - Countdown timer for Monitor mode (MM:SS format)
   - Thread-safe operation (GUI remains responsive during measurements)
   - Detailed error reporting with command logging

6. Desktop Integration
   - Created ZNLE6_Controller.desktop file for desktop launcher
   - Single-click access to GUI from desktop icon
   - Configured to use virtual environment Python interpreter

7. Environment Setup
   - Configured Python virtual environment (.venv)
   - Installed required dependencies: pyvisa, pyvisa-py, matplotlib
   - Updated GUI and desktop launcher to use venv Python path

8. File Organization
   - Created CSVs subdirectory for data files
   - Created Plots subdirectory for graph images
   - Updated default output paths to use organized folder structure
   - Both directories are created automatically if they don't exist

9. Monitor Mode Plot Enhancements
   - Calculate and display average amplitude over entire trace duration
   - Fixed y-axis scaling to average ± 5 dB for consistent trace comparison
   - Added average amplitude to plot title
   - Enables side-by-side comparison of different alignment configurations

10. GUI Refinements
   - Increased window size to 750x750 for better visibility
   - Fixed countdown timer display (changed to tk.Label for color support)
   - Added force display updates to ensure timer refreshes properly
   - Added debug logging for timer operation
   - Enhanced error reporting with detailed command logging
   - Redesigned output settings with separate directory pickers for CSVs and Plots
   - Simplified filename input to prevent overwriting previous datasets
   - Stop button available to cancel measurements in progress
   - Auto-opens generated plot in default image viewer after measurement completes
   - Real-time countdown timer (MM:SS format) during monitor mode
   - Default monitor frequency set to 900 MHz (center of frequency range)

11. Usage
   - Launch from desktop: Double-click "ZNLE6 Controller" icon
   - Launch from terminal: python3 znle_gui.py
   - Configure settings in GUI fields
   - Click mode button to start measurement
   - View real-time progress in status window
   - CSV files saved to: Antenna_Aligner/CSVs/
   - Plot images saved to: Antenna_Aligner/Plots/

Dependencies:
   - tkinter (Python standard library)
   - threading, subprocess, os (Python standard library)


Raspberry Pi Server Integration - 02/04/26
==========================================

1. Created pi_tcp_server.py - WebSocket + TCP Control Server
   - Dual-protocol server listening on port 8000
   - WebSocket endpoint at /ws for Flutter app communication
   - Plain TCP interface for testing and control commands
   - Handles concurrent WebSocket and TCP connections
   - Asynchronous architecture using asyncio

2. Created pi_websocket_server.py - Dedicated WebSocket Server
   - Real-time RSL (Received Signal Level) data streaming from VNA
   - Provides live signal strength updates to Flutter client
   - Command handling from Flutter app
   - Integration with PyVISA for VNA communication
   - Fallback to simulated data when VNA unavailable

3. Server Features - Command Support
   - PING/PONG for connection testing
   - STATUS command for server health checks
   - START_SWEEP command to initiate VNA measurements
   - Continuous data streaming during active sweeps
   - JSON-formatted messages for structured communication

4. Systemd Service Integration
   - Created pi_tcp_server.service for automatic server startup
   - Configured as system service with automatic restart on failure
   - RestartSec=5 for quick recovery from errors
   - Runs under sd2-group2 user with proper working directory
   - Enables headless operation on Raspberry Pi

5. Documentation
   - Created README_systemd.md with setup instructions
   - Includes service installation and management commands
   - Manual testing procedures before service deployment
   - Commands for viewing logs with journalctl

6. Flutter App Updates
   - Added WebSocket client integration in lib/vna_service.dart
   - Real-time VNA data monitoring in Flutter application
   - Enhanced connection checking for WebSocket availability
   - Two-sided UI support for azimuth/elevation control
   - IP address configuration for Pi connection

Dependencies Added:
   - websockets (Python package for WebSocket protocol)
   - asyncio (Python standard library)
   - pyvisa, pyvisa-py (for VNA instrument control)


Changes to znle_pyvisa.py Script - 01/28/26
============================================

1. VNA Port Configuration
   - Added CALC:PAR:PORT 1 command to explicitly set VNA to read from port 1

2. Plotting Functionality
   - Added matplotlib library for data visualization
   - Added --plot command-line flag to enable graphing
   - Plots display amplitude (dB) vs frequency (GHz)
   - Graphs are saved as PNG files (e.g., trace_plot.png) in the working directory

3. Time-Series Monitoring Mode
   - Added --monitor flag for antenna alignment mode
   - Plots amplitude vs time instead of frequency
   - Takes repeated measurements over specified duration (default: 60 seconds)
   - Monitors amplitude at a specific frequency or tracks maximum across sweep
   - Added --monitor-freq flag (default: 1 GHz)
   - Added --duration flag to set monitoring time (default: 60 seconds)
   - Added --interval flag to set measurement interval (default: 1 second)

4. Hardware Configuration Updates (01/28/26)
   - Changed default frequency range to 900-1500 MHz (NanoVNA-H4 output range)
   - Changed default measurement parameter from S21 to S11
   - Updated for new receiver configuration: external signal feeds directly to ZNLE6 port 1
   - New hardware: NanoVNA-H4 transmitter + RFSpace UWB antenna (900-12000 MHz)
   - New receiver: Pasco WA-9800a connected directly to ZNLE6 port 1

5. Usage Examples
   - Single sweep with plot: python znle_pyvisa.py --plot
   - Monitor at 1 GHz for 60 seconds: python znle_pyvisa.py --monitor --plot
   - Monitor for 2 minutes: python znle_pyvisa.py --monitor --plot --duration 120
   - Custom monitoring frequency: python znle_pyvisa.py --monitor --plot --monitor-freq 1.2e9
   - Frequent measurements: python znle_pyvisa.py --monitor --plot --interval 0.5

Dependencies Added:
   - matplotlib (installed via pip)
   - time, datetime (Python standard library)
